# Adversarial PRD Review — 017-tdd-discipline

> Generated by Codex adversarial review pass. Read-only; no PRD files were modified.

---

## Vision-to-Goal Trace

**Interview Goal** (`017-tdd-discipline-interview.md:7`):
> "reframe `specflow:develop` Phases D / E / F as Pocock's Red → Green → Refactor, make tests-first mandatory for the Yellow lane and configurable for the Green lane, surface explicit `red` / `green` / `refactor` markers in the per-task manifest, and extend `specflow:test` so the Red phase can write a failing test plan artefact without executing"

**PRD Vision** (`017-tdd-discipline-prd.md:12`):
Matches the main goal, but adds that markers let "Gate 5 reviewers" see whether the cycle held. This conflicts directly with the PRD's own goal/non-goal and R10, which state markers are "not Gate 5 input" (`prd.md:27`, `:36`, `:83-85`).

**Severity: MAJOR**

---

## Requirement Traceability Audit (R1–R12)

| Req | Trace present? | Serves goal present? | Verdict | Notes |
|-----|---------------|---------------------|---------|-------|
| R1  | Yes | Yes | OK     | Directly traces to interview Round 2 (`prd.md:46-48`). |
| R2  | Yes | Yes | RISK   | Trace exists, but renaming current Phase E conflicts with current Gate 5 location (`develop/SKILL.md:446-533`; `prd.md:50-52`). |
| R3  | Yes | Yes | RISK   | Trace exists, but renaming current Phase F conflicts with current Verifier/PR phase (`develop/SKILL.md:537-633`; `prd.md:54-56`). |
| R4  | Yes | Yes | OK     | Direct trace to Round 3 (`prd.md:58-60`). |
| R5  | Yes | Yes | OK     | Direct trace to Rounds 1/2 (`prd.md:62-64`). |
| R6  | Yes | Yes | WEAK   | Config behavior is traced, but testability depends on R9/AC-7 marker storage while 019 is absent (`prd.md:66-68`, `:78-81`, `:115-117`). |
| R7  | Yes | Yes | OK     | Preserves current Red-lane human-led behavior (`prd.md:70-72`; `develop/SKILL.md:430-436`). |
| R8  | Yes | Yes | OK     | Existing `--plan-only` slot verified (`test/SKILL.md:39`, `:51`, `:214`; `prd.md:74-76`). |
| R9  | Yes | Yes | WEAK   | Schema dependency is named, but stub section format is not stated (`prd.md:78-81`). |
| R10 | Yes | Yes | RISK   | Conflicts with Vision wording that Gate 5 reviewers can see markers (`prd.md:12`, `:27`, `:36`, `:83-85`). |
| R11 | Yes | Yes | RISK   | Trace exists, but "new file is by definition new behaviour" is asserted rather than validated (`prd.md:87-89`). |
| R12 | Yes | Yes | OK     | Codebase-context trace, not direct user goal trace, but acceptable (`prd.md:91-93`; `CORE_PRINCIPLES.md:1-76`). |

---

## AC Binary-Pass/Fail Audit (AC-1–AC-11)

| AC    | Binary? | Issue (if any) |
|-------|---------|----------------|
| AC-1  | Yes     | Literal heading grep is observable (`prd.md:97-98`). |
| AC-2  | Yes     | Literal invocation check is observable (`prd.md:100-101`). |
| AC-3  | Mostly  | "Contains a verify step" is observable, but behavior is documentation-level unless a fixture exercises lane/config branches (`prd.md:103-104`). |
| AC-4  | Yes     | Requires explicit listed bounds (`prd.md:106-107`). |
| AC-5  | Partially | "Schema or documented expectations in the relevant skill" leaves the accepted location ambiguous (`prd.md:109-110`). |
| AC-6  | Yes     | Documentation check is observable (`prd.md:112-113`). |
| AC-7  | Partially | One example using one outcome note can pass without covering both `skipped (config)` and `skipped (trivial)` (`prd.md:115-117`). Stub section format is not defined. |
| AC-8  | Partially | References "Phase G" even though current Verifier/PR is Phase F (`prd.md:119-120`; `develop/SKILL.md:537`). Absence-based check is fragile. |
| AC-9  | Yes     | Line count, section name, quote inclusion are observable (`prd.md:122-123`). |
| AC-10 | Mostly  | Absence of a new TDD verify step in Red-lane handoff is observable but negative-only (`prd.md:125-126`; `develop/SKILL.md:430-436`). |
| AC-11 | Partially | "Compiles to a hard-block" lacks the fixture/compiler contract; emitted message is binary only if fixture execution is defined (`prd.md:128-129`). |

---

## Scope-Boundary Check

- **[CRITICAL]** R2/R3 implicitly displace existing Gate 5 and Verifier phases by renaming current Phase E to Green and current Phase F to Refactor, while R10 still cites Gate 5 at `develop/SKILL.md:446-533` and AC-8 references a non-existent Phase G (`prd.md:50-54`, `:83`, `:119`; `develop/SKILL.md:446`, `:537`).
- **[MAJOR]** Vision says Gate 5 reviewers can see marker audit signal, while Goals/Non-goals/R10 say markers are not Gate 5 input (`prd.md:12`, `:27`, `:36`, `:83-85`).
- **[MINOR]** R8 says Green flips status after `vitest` "or the configured runner" confirms pass; this does not directly change runners, but the runner-selection source is not tied to existing `test/SKILL.md` runner detection (`prd.md:74`; `test/SKILL.md:101-108`).
- **[OK]** R6 applies only inside Green and does not alter lane assignment precedence as stated (`prd.md:33`, `:66-68`, `:70-72`).

---

## Targeted Tensions (T1–T4)

### T1 — R6 testability without 019: PARTIALLY-DEFINED

R6 requires `develop.tddRequired` and `red: skipped (config)` when Green opts out (`prd.md:66-68`). R9 says markers live in 019's future per-task manifest, with a stub section fallback if 019 has not landed (`prd.md:78-81`). AC-7 only requires an example using one documented outcome note, so it can pass with `passed` only and never prove the config-skip path (`prd.md:115-117`). That leaves R6 only partially binary-testable today.

### T2 — R3 false positives: RISK

R3 and R11 hard-block any new file during Refactor and route to `specflow:scope-change` (`prd.md:54-56`, `:87-89`). The escape hatch is stated, not merely implied. However, the PRD asserts a new helper file is "by definition new behaviour" (`prd.md:87`) and does not state how to distinguish legitimate structural extraction from scope expansion. **HYPOTHESIS:** this can produce false positives for co-located helper extraction that leaves external interface and test surface entirely unchanged.

### T3 — R10 vs manifest markers: RISK

R10 says Gate 5 reviews the combined Red+Green+Refactor diff and does not replay the transition (`prd.md:83-85`). That protects Gate 5 from becoming three sub-reviews, but it does not guard against reviewer under-scrutiny based on marker labels. The risk is sharpened by the Vision saying Gate 5 reviewers can see whether the cycle held (`prd.md:12`), while R10 says markers are "not Gate 5 input" (`prd.md:83-85`). The PRD does not contain a guardrail that prevents a reviewer from using `green: passed` to deprioritise Green-commit scrutiny.

### T4 — AC-7 coverage gap: PARTIALLY-DEFINED

AC-7 covers a manifest fragment with three markers and "one of" the documented outcome notes (`prd.md:115-117`). It therefore does not require both skip cases: `red: skipped (config)` from R6 and `refactor: skipped (trivial)` from R4 (`prd.md:58-60`, `:66-68`). **Missing ACs:** (a) an observable case for Green-lane `tddRequired: false` producing `red: skipped (config)`; (b) an observable trivial task producing `refactor: skipped (trivial)`.

---

## Unstated Assumptions

- The future 019 manifest stub section in `{NNN-slug}-tasks.md` has a parseable shape today; underlies R9/AC-7 (`prd.md:78-81`, `:115-117`).
- Current Phase E/F can be renamed to Green/Refactor without losing Gate 5/Verifier semantics; underlies R2/R3/R10/AC-8 (`prd.md:50-54`, `:83`, `:119`).
- "Primary AC" is unambiguous for every task; underlies R8/AC-6 (`prd.md:74`, `:112`).
- A new file during Refactor always equals new behavior; underlies R3/R11/AC-11 (`prd.md:54-56`, `:87-89`, `:128-129`).
- Gate 5 reviewers will not use cycle markers to reduce review depth; underlies R10 (`prd.md:83-85`).
- `admin/config.json` schema location or equivalent documented expectation is discoverable; underlies R6/AC-5 (`prd.md:66-68`, `:109-110`).

---

## Top Findings Summary

| # | Severity | Finding | Affected artefact ref |
|---|----------|---------|----------------------|
| 1 | [CRITICAL] | Phase mapping conflict: R2/R3 rename current Gate 5/Verifier phases while R10 still depends on current Gate 5 | `prd.md:50-54`, `:83`, `:119`; `develop/SKILL.md:446`, `:537` |
| 2 | [MAJOR]   | Vision says Gate 5 reviewers can see marker audit signal, but R10/non-goals say markers are not Gate 5 input | `prd.md:12`, `:27`, `:36`, `:83-85` |
| 3 | [MAJOR]   | AC-7 does not make R6 binary-testable before 019 lands; stub-section fallback lacks a concrete schema | `prd.md:66-68`, `:78-81`, `:115-117` |
| 4 | [MAJOR]   | AC-7 can pass without covering either skip outcome required by R4/R6 — two ACs are missing | `prd.md:58-60`, `:66-68`, `:115-117` |
| 5 | [MAJOR]   | AC-8 cites non-existent Phase G and checks the wrong phase boundary | `prd.md:119`; `develop/SKILL.md:537` |
| 6 | [MINOR]   | R11's "new file is by definition new behaviour" is asserted, creating false-positive risk for structural helper extraction | `prd.md:87-89` |
| 7 | [MINOR]   | AC-5's accepted schema location is ambiguous | `prd.md:109-110` |


— User reviewed; revisions applied autonomously per session-start authorisation, 2026-05-07.
